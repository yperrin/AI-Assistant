# Experiments Sandbox

This is your playground for new code, scratchpads, or proofs of concept.

## Workflow
1. Create a subfolder for your new experiment.
2. Build and iterate freely here without worrying about perfect structure.
3. If the experiment becomes a solid tool or pipeline, move it to the `tools/` directory (like `notion_sync`).
